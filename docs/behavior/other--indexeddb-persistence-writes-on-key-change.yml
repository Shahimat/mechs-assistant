id: other--indexeddb-persistence-writes-on-key-change
kind: behavior
namespace: persistence
entities: [other--indexeddb-persistence, other--favorites]
title: Middleware записывает в IndexedDB только те persistKeys, значение которых изменилось

scenarios:
  - given: `toggleFavorite(K)` вызван, `favorites` в состоянии меняется
    when: setWithPersistence сравнивает next[key] и current[key] для каждого persistKey
    then: |
      для `favorites` (значение изменилось) вызывается
      `setToIndexedDB(db, 'robots-store', 'favorites', <newArray>)`.

  - given: `initializeRobots` пишет `robots`, `isLoading`, `error`
    when: setWithPersistence обрабатывает патч
    then: |
      `robots`/`isLoading`/`error` не входят в persistKeys — запись
      в IDB для них НЕ выполняется, `set(partial)` применяется как обычно.

  - given: запись в IDB упала (например, из-за ограничения квоты)
    when: catch в цепочке `.then(setToIndexedDB).catch(...)`
    then: ошибка логируется в `console.error`, состояние в памяти всё равно обновлено.

verified: true
verified_at: 2026-07-12
verified_note: |
  Проверено чтением кода: `setWithPersistence` (`indexedDBMiddleware.ts:156-183`)
  вычисляет `nextState`, для каждого key из persistKeys сравнивает
  `next[key] !== current[key]` и только при изменении вызывает
  `setToIndexedDB(...)` через `initializeDB().then(...)`. persistKeys
  = [`favorites`] (`store.ts:28`), поэтому `robots`/`isLoading`/`error`
  из `initializeRobots` не попадают в IDB — обычный `set(partial)`
  их применяет. Ошибка записи ловится `.catch((err) => console.error(...))`
  (`174-178`), состояние в памяти обновляется независимо через
  безусловный `set(partial, replace)` в конце (`182`).
