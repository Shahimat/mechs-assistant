# Журнал решений

Формат: «когда» и «почему». Текущее состояние — в `docs/entities/*.yml`,
`docs/behavior/*.yml` и коде.

- 2026-07-19 · в систему целей введён слой **программ** — крупная инициатива с общей техбазой и набором подцелей заводится как `program--<key>.yaml` (`kind: program`) с собственным платформенным слоем задач (`tasks`), подцели — обычные `goal--<key>.yaml` с полем `program: <key>`, в `goals.yaml` вложены под `subgoals` (порядок = приоритет); прежняя единая цель `clan-operations-platform` разложена в программу **cop** + 7 подцелей — `cop-assistant` (помощник по рутине игры + мигрированный сюда парсер инвентаря, была отдельная цель `inventory-parser`), `cop-warehouse` (WMS-склад), `cop-personal-planner`, `cop-debts`, `cop-calc-integration`, `cop-team-goals`, `cop-voice-chat`; платформенные задачи (единый codebase, Cloudflare Worker+KV, generic-транспорт синхронизации, ручная токен-авторизация) вынесены в `tasks` самой программы, подцели ждут их через блокер «платформа программы» вместо зависимости на подцель; калькуляторы (`calculator`, `combat-calculator`) остались самостоятельными целями, связь с кланом — через подцель `cop-calc-integration`; conventions `goals` (+секция «Программы») и `goal-consistency` (+единое множество проверки для подцелей и целей) расширены · COP по объёму перерос одну цель: разные подцели имеют независимые приоритеты, статусы и жизненные циклы, но общую техбазу — плоский список целей это не выражал, а вложенность программа→подцели даёт и приоритезацию, и переиспользование платформенного слоя
- 2026-07-18 · `merge-develop-to-main.yml` — снят nightly-cron (`0 3 * * *`), остался только `workflow_dispatch`; merge develop → main теперь запускается вручную · автомерж по ночам собирал в main непроверенные коммиты develop без явного решения — контроль момента влития важнее автоматизации
- 2026-07-18 · система задач/стратегии переустроена — прежние `context/views/active.todo` (рабочий срез) + `context/roadmap.md` (стратегия) слиты в единую **структуру целей**: индекс `context/views/goals.yaml` (порядок = приоритет цели) + `context/views/goal--<key>.yaml` (why + description + задачи по секциям blocker/high/medium/low, порядок в секции = приоритет задачи); 7 целей — inventory-parser, clan-operations-platform, calculator, catalogs, combat-calculator, infra, improvements; заведены conventions `goals` (устройство, статусы active/planned/paused, жизненный цикл «решена → CHANGELOG + удалить файл») и `goal-consistency` (кросс-целевая проверка непротиворечивости при создании/уточнении цели), обе вшиты в сценарии plan/execute/report `workflow.md`; реализованная архитектура из roadmap разнесена по домам (git-процесс/ruleset/MERGE_PAT → `workflow.md`, хранение+версионирование данных → convention `data-overlay`, ruleset+merge-ручка → manual `setup-github-workflow`); все ссылки (CLAUDE.md, project.yml, index.yml, conventions, entities/behaviors, README) перенаправлены · «фазы»-нумерация (Phase A/M1/…) заменена на порядок в очереди; «done»-статус у цели убран (решённая цель не существует, её история — в CHANGELOG/git), чтобы файлы целей не копили закрытую историю; verification-механизм не даёт держать противоречивые цели («настроить бэкенд» vs «удалить бэкенд»)
- 2026-07-16 · `apps/inventory-parser/` Phase K/L1/L2 закрыты кодом (L2 ждёт разовой ручной настройки ключа юзером). Phase K — кнопка «Экспортировать диагностику» → ZIP через fflate: meta.json + screenshot-raw + screenshot-annotated (общий util `annotateCanvas.ts` переиспользован CaptureView и export'ом) + cells/_-icon.png + cells/_-count.png + console.log (глобальный перехват console.warn/error в App.tsx, ring-buffer 500 записей). Phase L1 — триггер workflow расширен с `workflow_dispatch` на `push` в develop/main с path-фильтром `apps/inventory-parser/**`; concurrency-группа зависит от ref+event, cancel-in-progress только на develop-push. Phase L2 — Rust плагины `tauri-plugin-updater` + `tauri-plugin-process` подключены; `tauri.conf.json` с `updater.endpoints` на GitHub Releases latest + placeholder pubkey; компонент `UpdateBanner` (check при монтировании, банер «Доступна версия X.Y.Z» → downloadAndInstall + relaunch, прогресс скачивания); workflow разделён на `build-dev` (артефакты 30 дней, как раньше) и `release` (только push в main, `tauri-apps/tauri-action@v0` подписывает + создаёт Release с тегом `inventory-parser-v0.1.<run_number>` + latest.json манифестом для updater'а). Manual `docs/manuals/setup-tauri-updater-signing.md` с шагами: `signer generate` → pubkey в конфиг → приватный ключ + пароль в GitHub secrets `TAURI_SIGNING_PRIVATE_KEY[_PASSWORD]` · закрывает цикл «правка → CI → скачивание .msi → установка» на «правка → merge в main → банер в приложении → клик Обновить», ручных действий на итерацию 1 вместо 4-5
- 2026-07-16 · MVP3 переформулирован: «клановый склад на shared BaaS backend» → **Clan Operations Platform (COP)**, Tauri-only, self-hosted master-БД на диске владельца клана + Cloudflare Workers KV как outbox/snapshot cache; никакого сервис-провайдерского backend'а, нулевые операционные косты; расширенный scope — не только склад, а склад + заявки на выдачу + двухуровневый admin (владелец + доверенные лица с custody-механикой) + цели клана + интеграция с калькулятором (MVP2); публичный SPA продолжает жить без изменений как справочник + анонимный калькулятор; персональные bearer-токены вместо Google OAuth; разбито на 4 подфазы M1-M4 (основа WMS → two-host → заявки → цели+калькулятор); ПРЕДУСЛОВИЕ — переезд `apps/inventory-parser` на единый codebase с публичным SPA (общий React + Tauri-only роуты + Rust-мосты); порядок работ — сначала добить парсер (Phase I-L), потом единый codebase, потом M1 → M4. Полностью новая MVP3-секция в `context/roadmap.md`, детальные подфазы в `context/views/active.todo` блок `cop:` · причины разворота: (1) OAuth+JWT+whitelist оказались over-engineering для клана 5-20 человек — bearer-токенов достаточно; (2) юзер хочет ownership данных на своём диске, а не «в Cloudflare»; (3) объединение inventory-parser + WMS + личный калькулятор в один Tauri-инструмент устраняет дублирование auth и данных между двумя приложениями; (4) КV-outbox снимает требование always-on host — владелец включает приложение при возможности, собирает pending за N дней offline'а
- 2026-07-16 · `apps/inventory-parser/` реальный тест на Windows выявил три класса проблем и правки — (1) оверлей визуализации: CaptureView перерисован на canvas, поверх скрина рисуется красная рамка вокруг найденного угла + жёлтая сетка ячеек с номерами, строчка статуса «рамка найдена: x=…, y=…, conf=XX%», без переснятия обновляется при изменении GridConfig — калибровка сетки перестала быть слепой; (2) `DEFAULT_GRID` пересобран под реальную геометрию инвентаря 3×4@48 (вместо 4×6@35 из пустого dev-теста), т.к. игра массово идёт на 1920×1080; (3) `recognizePage` возвращает `{ items, corner }` вместо `Recognized[]` — угол экспонируется наружу для UI-оверлея · без визуализации юзер видел «5 распознано из 13 занятых» без понимания что не так; красная+жёлтая сетка сразу указывает на источник проблемы (не тот угол vs не тот размер vs OCR молча скипает)
- 2026-07-16 · `apps/inventory-parser/` `.npmrc` копия внутри подпроекта — root `.npmrc` с явным `registry=https://registry.npmjs.org/` не подхватывается при `npm ci` из `apps/inventory-parser/` (npm cli не walks-up, ищет только cwd + `~/.npmrc` + global); на CI без `~/.npmrc` это ломало install; лечится физической копией `apps/inventory-parser/.npmrc`, синхронизировать с root вручную (пока копия одна — чек-лист лёгкий) · env-override `NPM_CONFIG_USERCONFIG` явно отвергнут — прятать конфиг в workflow-файле хуже, чем самодокументирующий `.npmrc` рядом с package.json
- 2026-07-16 · `apps/inventory-parser/` Sharp вынесен в root `package.json` (devDependencies), `data/icons/**` → `apps/inventory-parser/src/generated/phash-index.json` теперь **коммитится в git** вместо генерации на CI; скрипт `build:phash` живёт в root, зовётся `npm run build:phash` — регенерь при обновлении иконок, коммить вместе с ними; в `apps/inventory-parser/package.json` Sharp удалён совсем, `build` = `tsc && vite build` без препроцессинга; заведена convention `monorepo-shared-config` (apps/* переиспользуют корневые тяжёлые build-time deps, детерминированные артефакты коммитятся) · Sharp на Windows-CI хронически ломал `npm ci` через баг `Exit handler never called!` (nmm/cli, нативные optional deps `@img/sharp-win32-*`); индекс детерминированный от версионируемых входов — держать его как build-artifact на CI бессмысленно, git решает
- 2026-07-16 · `merge-develop-to-main.yml` + `sync-sheets.yml` — checkout под fine-grained PAT `MERGE_PAT` (scope `Contents: Read & write` на репо), не `GITHUB_TOKEN`; ruleset main с bypass Write-role не пропускал прямые push от `github-actions[bot]` (bot != коллаборатор с Write-ролью, bypass покрывает только людей); PAT под учёткой admin-автора → push проходит как «автор», bypass срабатывает · без PAT nightly merge-develop и sync-sheets молча ломались бы при первом коммите после включения ruleset
- 2026-07-15 · `apps/inventory-parser/` Phase D закрыта — эталон угла UI-рамки инвентаря `src-tauri/assets/inventory-corner.png` захардкожен в бинарник через include_bytes!, Rust-команда `find_inventory_corner` через `imageproc::template_matching::match_template` (SumOfSquaredErrorsNormalized) ищет минимум в правой половине скрина; клиент рендерит сетку через `GridSettings` (offset_x/y, cell_size, cols, rows) с сохранением в tauri-plugin-store; полный pipeline screenshot → распознанные позиции работает end-to-end · standalone-парсер готов, дальше — реальный тест на Windows-сборке
- 2026-07-15 · `apps/inventory-parser/` Phase B-G реализованы — Rust-команда `capture_window(title_pattern)` (xcap, PNG в base64, без активации), React с хоткеями Alt+Q / Alt+Shift+Q / Alt+Ctrl+Shift+Q (одиночный кадр / серия страниц / закрыть серию); `scripts/build-phash-index.mjs` через sharp собрал 1025 иконок из `data/icons/` в 256-битный pHash-индекс (159 KB, gitignored, генерируется в CI); pipeline `phash.ts` + `ocr.ts` (tesseract.js для чисел) + `recognize.ts` (crop ячейки, matching, threshold 0.6) готовы; `mergeSeries.ts` дедуп по (catalog, item_key); UI `InventoryResult` с копированием в TSV/JSON/plain и подсветкой low-confidence. Phase D template matching UI-рамки — skeleton, ждёт эталон угла инвентаря + тестовый скрин игры от юзера · закрыт основной pipeline standalone-парсера; после активации Phase D одним куском заработает конвейер screenshot → распознанный список
- 2026-07-15 · заведён desktop-tool `apps/inventory-parser/` — Tauri 2.x + React 19 + Vite scaffold, плагины `global-shortcut` + `store`, Rust-крейт `xcap` для скринов окон по hwnd без активации, `image` + `imageproc` для будущего template matching UI-рамки инвентаря; GitHub Actions workflow `build-inventory-parser.yml` собирает .exe + .msi + NSIS-setup на windows-latest по `workflow_dispatch`; Phase A закрыта (scaffold + enumeration окон), впереди Phase B-G (capture + pHash + OCR) · автоматизация ввода прихода в клановый склад — юзер жмёт хоткей в игре, приложение снимает окно нужного меха, распознаёт инвентарь локально; standalone MVP, интеграция с backend MVP3 позже
- 2026-07-15 · заведена четвёртая GitHub Actions ручка `merge-develop-to-main.yml` — merge коммитов из develop в main через `git merge --no-ff`, триггеры `workflow_dispatch` + nightly cron `0 3 * * *`, early-exit если в develop нет новых коммитов (не создаёт пустой merge-commit, не триггерит deploy); при конфликте job падает — разрешать локально · автоматизация перехода develop→main без ручного `git merge` в терминале, cron покрывает случай «забыл влить перед деплоем»
- 2026-07-15 · защита ветки `main` от прямых push'ей — ruleset «main» с Enforcement Active (require PR + block force pushes + restrict deletions), bypass у роли Write со статусом Always (пропускает admin-автора и `GITHUB_TOKEN` sync-workflow'ов); заведена ветка `develop` как основная рабочая, в `main` через PR · подготовка к росту репы за счёт бот-коммитов и потенциальных коллабораторов — прямой push от людей блокируется, sync-workflow'ы продолжают работать без переделки на PR-cycle
- 2026-07-14 · зафиксировано разделение задач: convention `todo-split` — `active.todo` для dev/internal (агент+юзер, привязка к workflow.md), Sheets `todo`/страница `/todo` для product transparency (игроки); не смешивать, агент читает оба источника в сценариях report · один общий список путал бы аудитории и ломал совместимость с operational workflow
- 2026-07-14 · заведён публичный TODO-лист разработки — meta-каталог `data--todo-list` (лист `todo` в overlay-spreadsheet, dropdown-validation на priority/status), страница `/todo` с 4 секциями по приоритету, плитка «Планы разработки» на HomePage, кнопка «Предложить задачу» → Google Sheets; полный цикл автосинка через существующий `sync-sheets.yml` workflow · transparency-механика для игроков и точка обратной связи (юзеры видят «что делается / что запланировано», редакторы правят задачи в знакомой Sheets-среде)
- 2026-07-14 · **MVP1 закрыт** — 11 data-сущностей + 16 UI-роутов работают; blueprints (211 записей, 7 категорий включая 'mech' из паков), transformsFrom в weapons/equipment, upgrade+providesSkill в items, cross-catalog навигация (?open= deep-link + IngredientMiniCard + BlueprintChipList) во всех каталогах, композитинг blueprint-иконок с фоном-«планшетом» · достигнута цель MVP1: полный справочный слой с автопарсом + overlay, связный по ключам — фундамент для MVP2 калькулятора прокачки готов
- 2026-07-14 · старт подключения десятого (финального в MVP1) каталога — `data--blueprints-catalog` (~215 чертежей из 9 подстраниц вики) со встроенным рецептом (producesKey+producesCatalog+ingredients с cross-catalog resolve); параллельно расширены `data--items-catalog` (+subtype='upgrade' через seed 3950 «Установка навыков», overlay-поле providesSkill для механики «Энергетика +3»), `data--weapons-catalog` и `data--equipment-catalog` (+transformsFrom — цепочка преобразований Мортира ← Мортира 5 ← 10 …, данные из td «Преобразуется из» на страницах weapon/equipment); PairToggleGroup blueprints — 6 капсул (3 пары); items — 5 капсул (+«Апгрейды») · чертежи ↔ преобразования — параллельные механики (крафт с нуля vs апгрейд существующего), обе должны быть в MVP1 чтобы каталоги были полными и связными по ключам
- 2026-07-14 · старт подключения девятого (финального в MVP1) каталога — `data--skills-catalog` из shell переведён в реальную (12 записей: 5 навыков из wiki 2902 + 7 умений из 2905); нестандартная разметка вики (плоский HTML без h2/table) → полу-ручной парсер (SKILL_ENTRIES захардкожен, из HTML только description и perStatPercent); каталог **справочный** — модель числовых модификаторов (skill × level → множители к другим каталогам) сознательно вынесена в отдельную сущность `data--skill-modifiers` перед MVP2 · разделение факты/механика: сейчас есть карточки-описания с семантическими тегами `affects`, в MVP2 подключим формулы и слайдер уровня — так проще итерировать
- 2026-07-14 · старт подключения восьмого каталога MVP1 — `data--loot-catalog` из shell переведён в реальную (65 записей из wiki 3023/3027/20118: монстры 41 + кроты 14 + пираты 10), BFS с `ctx.source`; поле `sources: string[]` заложено на случай будущих пересечений (сейчас всегда одноэлементный); UI — SearchField + PairToggleGroup по source (3 капсулы), матч через `some()` · пересечений между тремя страницами на 2026-07-14 нет, но `string[]` в схеме — маленькая цена за готовность к пересечениям (например, «универсальный сертификат» на нескольких источниках)
- 2026-07-13 · старт подключения седьмого каталога MVP1 — `data--components-catalog` из shell переведён в реальную (32 записи из wiki 3955, гибридная модель): parser тянет name/weight/sellPrice/description/craftFromBlueprints; overlay-колонки `kind` (base|composite) и `recipe` (список входов) заведены в схеме и в Sheets, но пустые до ручного наполнения; UI — SearchField + PairToggleGroup по kind с auto-hide (не шумит пустыми капсулами до наполнения) · рецепты критичны для MVP2 калькулятора, но структура полей уже описана и стоит фиксировать в схеме сейчас, чтобы Sheets был готов к заполнению
- 2026-07-13 · старт подключения шестого каталога MVP1 — `data--ore-catalog` из shell переведён в реальную (8 записей из wiki 3036, все с CSS `wiki-item-type-cargo`); поле `foundIn: string[]` — простой список источников (без count/chance, в отличие от items — там LootDrop[] со сложной разметкой); UI минималистичный — без фильтров и поиска (8 записей помещаются на один экран), foundIn показан только в детализации · маленький каталог не оправдывает панель фильтров; сохраняем визуальный минимализм
- 2026-07-13 · старт подключения пятого каталога MVP1 — `data--items-catalog` из shell переведён в реальную (~54 записи, 7 subtype: pack/gift/energy/invis/repair/scanner/teleport); BFS с 7 seed'ами напрямую (корневая 3111 — оглавление, ссылается на подстраницы), `subtype` приходит через `ctx` резолвера, не через CSS (pack и gift разделяют `wiki-item-type-used-set-object`); UI — PairToggleGroup 4 капсулы, «главный» stat по subtype в карточке · items сильно разнообразнее ammo/equipment по полям (каждый subtype со своей спецификой) → плоская схема с subtype-specific optional полями, primary_stat зашит в docs
- 2026-07-13 · плитка «Валюта» снята с `CatalogsHubPage`, stub-роут `currencies-catalog` удалён; entity `data--currencies` осталась как ссылочная (боны/реглы упоминаются в `Price` типах Weapon/Ammo/Equipment/Robot) · тривиальный справочник «есть боны и реглы» не оправдывает отдельного view — снятие лишнего контента на хабе
- 2026-07-13 · старт подключения четвёртого каталога MVP1 — `data--ammo-catalog` из shell переведён в реальную (~58 записей из wiki 3661, все с CSS `wiki-item-type-bullet`, subtype-разбивки нет); ключевое поле схемы — `compatibleCategories: string[]` — массив CSS-slug'ов совместимого оружия (собирается из «Группа орудий» на вики через отдельные `<a>`-теги, маппится в те же 15 категорий, что у weapons); UI-фильтр PairToggleGroup строится по `compatibleCategories`, не по одиночному `family` · связь ammo ↔ weapons должна жить в данных, а не только в UI — иначе каталог получается изолированным списком без ответа «какой патрон к какому оружию»
- 2026-07-13 · «оборудование» раздроблено на 7 отдельных UI-каталогов (chips/shields/armour/accumulators/reactors/drills/cargos) — data-сущность `data--equipment-catalog` остаётся единой (1 JSON, 1 resolver, 1 сет колонок в Sheets, 1 store), UI-слой раздроблен на 7 view + 7 роутов + 7 плиток на CatalogsHubPage; избранное — общее для всех семи · «оборудование» как единая плитка получалось зонтичным для семантически разных сущностей (чипы ≠ броня ≠ буры), но семантика данных на вики едина → раздроб только на UI, не размножая инфраструктуру данных
- 2026-07-13 · миграция ESLint на flat config — eslint 8.57 → 9.39, `@typescript-eslint/*` 7 → unified `typescript-eslint` 8.63, `eslint-plugin-react-hooks` 4 → 7, `.eslintrc.json` заменён на `eslint.config.js`; попутно refactor двух `useEffect(setState)` в `LevelRangeFilter`/`useLevelRangeFilter` под новое правило `react-hooks/set-state-in-effect` · старый стек линта тормозил PostToolUse-хук (npx тянул eslint 10 без совместимого конфига), пора выравнивать
- 2026-07-13 · старт подключения третьего каталога MVP1 — `data--equipment-catalog` из shell переведён в реальную (7 подтипов на 4 семьи: computer/extractor/cargo/accumulator/generator/armour/shield; family+subtype берутся из двухчастного CSS-класса `wiki-item-type-<family>-<subtype>`, ~174 записи), заведён `view--equipment-catalog` + 4 behavior; парсер расширяется через новый resolver, UI собирается из catalog-примитивов с PairToggleGroup по subtype (раскладка капсул по family) · оборудование = 4 корневых wiki-страницы (3682/3657/3106/3108), объединённых одной сущностью через subtype-фильтр — не заводить 4 отдельных каталога
- 2026-07-12 · weapons каталог подключён — parser-wiki обобщён на lib/(fetch, html, icons, bfs) + resolvers/(robots, weapons); build-data и setup-sheets-columns работают по списку каталогов из общего `scripts/catalogs.config.ts`; parser сгенерировал 142 weapon-записи из 3341 и подстраниц категорий (missile/cannon/machinegun/…); UI собран из catalog-примитивов, без фильтров на первый заход, роут /catalogs/weapons-catalog живой; в рамках рефактора вынесены общие `src/stores/indexedDBMiddleware.ts`, `src/components/catalog/OverlayBadge.tsx`, `src/types/common.ts` (Price+OverlayMeta), `src/utils/overlay.ts` типизирован generic-ом по `_meta` · паттерн подключения каталога подтверждён на втором каталоге, оставшиеся 9 накатываются через тот же конвейер
- 2026-07-12 · старт подключения второго каталога MVP1 — `data--weapons-catalog` из shell переведён в реальную (схема Weapon: stats.damageMin/Max, range, energyConsumption, rateOfFire, ammo, durability, weight; category из CSS `wiki-item-weapon-<subtype>`, group из поля таблицы), заведён `view--weapons-catalog` + 3 behavior; парсер вики обобщается на lib/+resolvers/ гибридно (сохраняет пилот мехов), build-data + parser-google-sheets переводятся на N каталогов; UI weapons — все элементы без фильтров на первый заход, фильтры настраиваются позже · тест переиспользования catalog-shell на втором каталоге плюс обобщение конвейера данных под 9 оставшихся
- 2026-07-12 · CatalogsHubPage расширен на 11 плиток с webp-иконками; 10 из 11 каталогов пока рендерят CatalogStubPage («в разработке»); assets разделён на `assets/raw/` (исходники) и `assets/images/` (готовые webp), правило закреплено в convention `assets-layout`, alias `@img` заведён · заготовка полной навигации на 11 каталогов + чёткое разделение сырья и продовых ассетов на будущее
- 2026-07-12 · переход на многостраничку — react-router-dom@7, `layout--app-shell` (Header + breadcrumbs + Outlet), маршруты `/` → `view--home`, `/catalogs` → `view--catalogs-hub`, `/catalogs/mechs-catalog` → `view--robots-catalog`; SPA-fallback на GitHub Pages через `404.html`; catalog-примитивы (`other--catalog-shell`) выделены пораньше по решению юзера, до второго каталога · заготовка под 4 MVP-эпика (плитки на главной) и 10 будущих каталогов (плитки на хабе) через единый паттерн
- 2026-07-12 · закрыта пилот-цепочка MVP1 на мехах (подход C) — сквозной конвейер `wiki/Sheets → parsers → data/ + overrides/ → build-time merger → GitHub Pages` работает, деплой на shahimat.github.io/mechs-assistant/ стабилен (фавикон + PWA-манифест), sync-sheets триггерит deploy через `gh workflow run`, 6 pending behaviors переведены в `verified: true`; следующий фронт — раскатка на остальные 10 каталогов через тот же конвейер · подход C требовал полной обкатки пилота до расширения; после закрытия можно параллелить каталоги без риска переделывать инфраструктуру
- 2026-07-11 · хранилище данных — прямо в основном репо (вариант α), без S3/Synology; парсеры коммитят `data/*.json` + `data/icons/` + `data/overrides/*.yml`; deploy на GitHub Pages под префиксом `/mechs/`; workflow `sync-and-deploy` с ручным `workflow_dispatch`, доступ через Collaborators; заведены manuals `setup-google-sheets` и `setup-github-workflow` · S3/MinIO избыточны для нашего масштаба (10-50 MB), git даёт бесплатное версионирование и PR-review при синке
- 2026-07-11 · три ручки GitHub Actions вместо одной — `sync-wiki` открывает PR (парсинг вики нестабилен, нужен review), `sync-sheets` пушит в main (стабильно), `deploy` автомат по push в main; source калькулятора `mechs.txt` фиксирован как sanity-check при review sync-wiki PR · разный риск процессов требует разной модели контроля
- 2026-07-10 · overlay-источник переведён на Google Sheets — доступно для всех 11 каталогов; `parser--google-sheets` синк в data/overrides/*.yml; merged JSON несёт `_meta.overlayFields/overlayUpdatedAt/overlaySource` для UI-подсветки уточнённых полей; контекстные вариации (заводы v2/v3, лут по локациям) отложены до MVP2/MVP4 · игроки-редакторы могут вносить уточнения в знакомой среде, не только автор
- 2026-07-10 · MVP1 расширен до полного справочного слоя — 11 data-сущностей (мехи, weapons, equipment, ammo, items, loot, blueprints, ore, components, currencies, skills), универсальный `parser--wiki`, гибрид парсер+overlay для loot/components, convention `data-overlay` · «каталог мехов» был слишком узкой формулировкой; калькулятору и складу нужен полный слой
- 2026-07-10 · эпик «Данные»: Вариант B — build-time bundle данных из S3-совместимого self-hosted Synology · разделение приватного архива и публичных данных, детали настройки — отдельной фазой перед парсером
- 2026-07-10 · визуал MVP1-3 — на MUI defaults без выделенной дизайн-фазы; возврат к дизайну в MVP4 (radar/spec-sheet) · юзер валидировал результат feat #1, лишний цикл не нужен
- 2026-07-10 · спецификация переведена на MVP1 — снос `robots-grid` и `base-robot-comparison`, ввод `view--robots-catalog`, convention `entity-naming` (префиксы view/data/parser/other) · открываем реализацию карточного каталога как замены таблице
- 2026-07-10 · заведён `context/roadmap.md` со стратегией MVP1-4 + сквозными эпиками, зафиксированы аудитория/позиционирование и внешние источники в `project.yml` · оформили общее видение до старта первого MVP
- 2026-07-09 · синхронизация с контрактом v2 (6f002e3a) — добавлен blocking-question, active.todo переведён на приоритеты · выравниваем правду с обновлённым upstream
- 2026-07-05 · синхронизация с operational contract v2 · подхват CHANGELOG.md, механизма модулей расширения, sync-архитектуры через prompts/index.yml + SHA-checkpoint, curl-only fetch, страховочного hook блокировки записи в memory
