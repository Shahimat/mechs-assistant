# Формат: `namespace: суть → пункт(ы) → блокеры/зависимости`
# У проекта пока нет manuals — контракт допускает отсутствие manual,
# если процедура тривиальна и покрыта README/package.json.
# Задачи сгруппированы по приоритету; порядок секций сверху вниз —
# от самого срочного к самому терпеливому. Внутри секции сверху вниз —
# от того, за что берёмся первым, к тому, что может подождать своей
# очереди.

## blocker

## high
# MVP1 · пилот-цепочка на мехах (подход C) — обкатка сквозной цепочки wiki→S3+Sheets→bundle→каталог
data: настройка Synology S3 — bucket, доступы (публичный readonly для данных мехов), версионирование, отдельная assets-директория для иконок → фаза docs → нет (параллельно с настройкой Sheets)
data: настройка Google Sheets — создать таблицу с листом mechs, Service Account в Google Cloud, ключ вне репо, доступ на редактирование доверенным игрокам → фаза docs → нет (параллельно с настройкой S3)
catalog: parser--wiki для мехов — минимальный js/ts-скрипт парсинга wiki page_id=3301/3088/3099/3102 + иконки, выгрузка в S3 → фаза feat → data: настройка Synology S3
catalog: parser--google-sheets для листа mechs — тянет через API v4 в data/overrides/mechs.yml → фаза feat → data: настройка Google Sheets
data: build-time bundle-скрипт — тянет data/robots.json и иконки из S3, мержит с overrides, сохраняет _meta.overlayFields/overlayUpdatedAt/overlaySource → фаза feat → parser--wiki + parser--google-sheets
ui: подсветка overlay-полей в view--robots-catalog — значок/подчёркивание + tooltip с датой обновления из _meta → фаза feat → build-time merger с meta-полями
ui: MVP1 chore — снести RobotsGrid.tsx, FavoriteStarCell.tsx, вычистить ag-grid-community/ag-grid-react/react-window из package.json, убрать CSS-импорты ag-grid из src/index.tsx → фаза chore → нет (можно параллельно с data-треком)
ui: MVP1 audit пилота — verified: true + verified_at на view--robots-catalog behaviors и data--robots-catalog behaviors после подключения парсеров → фаза audit → build-time bundle подключён

## medium
# MVP1 · остальные 10 каталогов — накатываются через тот же конвейер после обкатки пилота
catalog: parser--wiki расширить конфиг на остальные каталоги (weapons, equipment, ammo, items, blueprints, ore, currencies, skills, loot, components) → фаза feat → пилот-цепочка отработала
catalog: parser--google-sheets расширить на остальные листы Sheets → фаза feat → пилот-цепочка отработала
ui: обобщить RobotsCatalog/RobotCard/RobotDetail в generic-паттерн — после подключения второго каталога, чтобы дублирование стало видимым → фаза refactor → второй каталог подключён
ui: подключить view для остальных каталогов через generic-паттерн (weapons, equipment, ammo, items, loot, blueprints, ore, components, skills) → фаза feat → generic-паттерн выделен
data: программный бэкап Google Sheets → cron-скрипт в backups/gsheets/YYYY-MM-DD.json → фаза feat → возникает по факту инцидента или роста объёма
data: решить судьбу @tanstack/react-query — либо начать использовать (в parser? в S3 fetch build-time?), либо убрать → нет

## low
catalog: TS-приведение `robotsData as Robot[]` не даёт runtime-валидации — рассмотреть zod/valibot для JSON-схемы (актуально для всех 11 data-сущностей после подключения) → нет
