# Формат: `namespace: суть → пункт(ы) → блокеры/зависимости`
# У проекта пока нет manuals — контракт допускает отсутствие manual,
# если процедура тривиальна и покрыта README/package.json.
# Задачи сгруппированы по приоритету; порядок секций сверху вниз —
# от самого срочного к самому терпеливому. Внутри секции сверху вниз —
# от того, за что берёмся первым, к тому, что может подождать своей
# очереди.
#
# Это internal/dev-очередь. Product-facing задачи (планы, интересные
# игрокам) живут в отдельной системе: лист `todo` в Google Sheets →
# `data/overrides/todos.yml` → страница `/todo`. Разделение
# зафиксировано в convention `todo-split`.
#
# Рабочий цикл git (с 2026-07-15): все правки — в ветке `develop`,
# в `main` вливаем через PR (merge через веб-UI). `main` защищён
# ruleset'ом (require PR + block force pushes + restrict deletions);
# bypass у роли Write со статусом Always — пропускает admin-автора и
# `GITHUB_TOKEN` sync-workflow'ов, коллабораторов заставляет идти
# через PR. Автор технически может push'нуть в main, но по
# договорённости не делает. Подробнее — в roadmap.md, секция
# «Защита main от прямых push'ей».

## blocker

## high
# MVP1 закрыт 2026-07-14 — см. CHANGELOG. Все 11 data-сущностей + 16
# UI-роутов работают; cross-catalog навигация (deep-link + мини-карточки)
# подключена во всех каталогах. Следующая веха — MVP2 калькулятор
# прокачки; перед стартом нужна фаза docs для skill-modifiers.

catalog: [skill-modifiers] новая сущность перед MVP2 калькулятора — модель числовых модификаторов (skill × level → множитель к weapon.damage / robot.armor / sellPrice / ...), overlay-первичная (данных на вики нет, ручной ввод) → фаза docs → блокирует старт MVP2

## medium
# main-page-icons закрыт 2026-07-14: 4 webp в assets/images/main-icons/,
# 5 плиток на HomePage, 3 stub-роута через WipPage для будущих MVP-эпиков.

backend+auth: [MVP3 Клановый склад — единый скелет для всех приватных фич] СОГЛАСОВАННЫЙ ПЛАН (2026-07-14):
  СТЕК: Cloudflare Workers + D1 (SQLite) + Hono + Drizzle ORM + jose (JWT-валидация) + `@react-oauth/google` на клиенте. Всё в free tier ($0/мес для 15-30 юзеров, ~500-2000 req/день).
  АВТОРИЗАЦИЯ: Google OAuth id-token → валидация в Worker по Google JWKS → email → lookup в `members` table → role (viewer/editor/admin). Bootstrap: INITIAL_ADMIN_EMAIL в Wrangler-secret (юзер как единственный admin на старте), дальше — управление через admin UI. Whitelist живёт в БД, не в env.
  СТРУКТУРА: `backend/` папка в том же репо (`backend/src/{index,auth,db/schema,routes/*}.ts` + wrangler.toml). Deploy — GitHub Action `deploy-backend.yml` (wrangler deploy по push в main). Backend URL — `.workers.dev` subdomain (бесплатный, кастомный домен — позже).
  БЭКАПЫ: Cloudflare R2 (S3-совместимое хранилище, 10 GB free) — nightly workflow пишет `warehouse-backups/YYYY-MM-DD.json`. Плюс D1 Time Travel даёт 30 дней встроенных snapshot'ов.
  МАСШТАБИРОВАНИЕ: любая новая приватная фича (календарь событий, статистика, голосования) добавляется тем же паттерном — новая миграция D1 + routes в backend/ + страницы в src/, использует существующий auth и role-model.
  СВЯЗЬ С TODO: тот же auth-механизм (Google OAuth + email-whitelist) применить к кнопке «Предложить задачу» на /todo (пока показывается всем) — гейт по role=admin. Единый `useAuth()` hook.
  ФАЗЫ (последовательные, каждая — после явного «поехали»):
    Phase 0 (setup, ты вручную): Cloudflare account + `wrangler login`, Google Cloud Console → OAuth 2.0 Client ID (Web application, добавить origins localhost:3000 + shahimat.github.io) — это ОТДЕЛЬНАЯ credentials от существующего Service Account, приватный репо не нужен если R2 выбран для бэкапов.
    Phase 1 (docs, я): conventions `private-data` / `auth-model` / `backend-layer`; entities `backend--warehouse-api` / `data--warehouse` / `view--warehouse`; 5-8 behaviors; правки roadmap.md/project.yml/index.yml; 2 строки CHANGELOG.
    Phase 2 (feat, я): backend skeleton — Hono + D1 + `/health` + `/me`; JWT валидация; INITIAL_ADMIN bootstrap; локальный `wrangler dev` + deploy на CF.
    Phase 3 (feat, я): warehouse endpoints — income/withdraw/participants schema, все routes, role-based middleware, тесты (vitest).
    Phase 4 (feat, я): warehouse frontend — `@react-oauth/google` + auth-store + BackendClient + WarehousePage + плитка HomePage + гейт TODO-кнопки под auth-model.
    Phase 5 (feat, я): backup workflow в R2 + endpoint `/admin/backup`.
    Phase 6 (data, юзер+я): миграция из текущего Sheets → D1 через одноразовый скрипт.
    Phase 7 (audit, я): verified:true на behaviors + manual для админа + README section.
  → фаза docs → блокеры: (1) Phase 0 setup от юзера, (2) явный старт по каждой фазе последовательно
catalog: [weapons/equipment/items/components/loot/ore] прогонять sync:sheets и наполнять overlay в Google Sheets — сейчас все overlay пустые (0 with overlay); наполнение = ручная работа редакторов, повышение точности каталогов относительно вики → фаза feat → нет
refactor: [weapons/ammo] вынести `CATEGORY_LABELS` + `LABEL_TO_CATEGORY` из `WeaponsCatalog.tsx` в общую утилиту `src/utils/weaponCategories.ts` (сейчас дублируется в WeaponsCatalog и AmmoCatalog) → фаза refactor → нет

tauri-parser: [автоматическое распознавание инвентаря игры — Tauri Windows-приложение] СОГЛАСОВАННЫЙ ПЛАН (2026-07-15, уточнён):
  ЦЕЛЬ: юзер жмёт хоткей → приложение делает скриншот окна игры «Мехи.Земля» с указанным никнеймом меха, локально распознаёт инвентарь, показывает список `{name, count}` с кнопкой «Скопировать». Standalone на первой итерации; интеграция с backend MVP3 через POST /warehouse/income — второй фазой.
  СТЕК: Tauri (Rust shell + React WebView), плагины `tauri-plugin-global-shortcut` + `tauri-plugin-store`, `xcap` для скринов окна по hwnd (без переключения фокуса), `imageproc`/`opencv-rust` для template matching UI-рамки инвентаря, `phash-js` в клиенте для matching иконок против index из `data/icons/**/*.webp`, `tesseract.js` для OCR чисел под иконками.
  ВЫБОР ОКНА: у одного игрока может быть до 5 окон одного процесса игры (multi-boxing, 1 окно = 1 мех); ID меха — в заголовке окна (никнейм). Конфиг per-installation: `target_window_title_contains = "<никнейм>"`. Enumerate по title, взять первое совпадение. Ctrl+Alt+Q и merge окон НЕ нужны — только одно окно за раз.
  ХОТКЕИ: Alt+Q (снять целевое окно), Alt+Shift+Q (начать серию для пагинации инвентаря), Alt+Ctrl+Shift+Q (закрыть серию, слить страницы в один список).
  DETECT INVENTORY: уровень 3 полностью автомат — template matching UI-угла рамки инвентаря на каждом скрине. UI игры стабильный (10+ лет не менялся), калибровки не делаем. Если template перестанет находиться — правка шаблона, не логики приложения.
  ФАЗЫ (можно начинать сейчас, backend не блокирует):
    Phase A [✓ 2026-07-15]: Tauri shell setup — Tauri 2.x + React 19 + Vite scaffold в `apps/inventory-parser/`, плагины `tauri-plugin-global-shortcut` + `tauri-plugin-store`, capabilities настроены; GitHub Actions workflow `build-inventory-parser.yml` (windows-latest, workflow_dispatch, артефакты .exe + .msi + NSIS setup, retention 30 дней).
    Phase B [✓ 2026-07-15]: Window enumeration + capture — Rust-команды `list_windows` (перечисление) и `capture_window(title_pattern)` (снимок окна по title contains через xcap, PNG в base64, не активирует окно). React: компоненты WindowList + CaptureView, хоткей Alt+Q через `@tauri-apps/plugin-global-shortcut`; handler'ы обновляются через useRef чтобы читать актуальный state.
    Phase C [✓ 2026-07-15]: pHash index иконок — `scripts/build-phash-index.mjs` через sharp собирает `data/icons/**/*.webp` (1025 иконок) в 16×16 grayscale → 64 hex/entry → `src/generated/phash-index.json` (159 KB). Скрипт wired как `npm run build:phash`, автоматически запускается перед `vite build` (значит и перед `tauri build` через beforeBuildCommand). Index gitignored — генерируется в CI.
    Phase D [✓ 2026-07-15]: template matching UI-рамки инвентаря. Эталон в `src-tauri/assets/inventory-corner.png` (~15×15 px верхнего-левого уголка) захардкожен в бинарник через include_bytes!. Rust-команда `find_inventory_corner(png_base64) -> Option<{x, y, template_width, template_height, score}>` через `imageproc::template_matching::match_template` с методом SumOfSquaredErrorsNormalized; ищет минимум только в правой половине скрина (инвентарь всегда справа); фильтр по score > 0.5 отсекает false-positive. Плюс диагностическая команда `debug_visualize_corner` — возвращает PNG с нарисованным красным прямоугольником вокруг найденной области. Разбиение на ячейки — на клиенте через GridConfig (offset_x, offset_y, cell_size, cols, rows) с ручной калибровкой через `GridSettings` (сохранение в tauri-plugin-store, дефолты — cell_size=35, cols=4, rows=6, offset=8).
    Phase E [✓ 2026-07-15]: pHash matching + OCR. Реализовано: `pipeline/phash.ts` (computePHash 16×16 grayscale через canvas + matchNearest с фильтром по каталогам INVENTORY_CATALOGS + Hamming distance), `pipeline/ocr.ts` (tesseract.js worker для чисел, whitelist 0-9, PSM 7), `pipeline/recognize.ts` (crop иконка/число из ячейки, pHash+OCR, threshold confidence≥0.6). Работает сквозным конвейером после активации Phase D.
    Phase F [✓ 2026-07-15]: серия страниц пагинации. Хоткеи: `Alt+Shift+Q` (очередная страница серии), `Alt+Ctrl+Shift+Q` (закрыть серию и слить). `pipeline/mergeSeries.ts` дедуп по (catalog, item_key), суммирует count, берёт max confidence.
    Phase G [✓ 2026-07-15]: UI финализация — компонент `InventoryResult`: таблица (иконка/item_key/catalog/count/confidence), фильтр «показывать low-confidence», три формата экспорта (TSV/JSON/plain), кнопка «Скопировать» через navigator.clipboard, подсветка low-confidence строк.
    Phase H (позже, после закрытия MVP3 backend Phase 3): интеграция с POST /warehouse/income (Google OAuth токен, отправка на Cloudflare Worker).
  → фаза feat → нет блокеров; следующее — реальный тест на Windows-машине (сборка через GitHub Actions → установить .msi → снять окно игры → откалибровать GridSettings под конкретное разрешение).
data: программный бэкап Google Sheets → отдельный GitHub Actions workflow, дампы в backups/gsheets/YYYY-MM-DD.json → фаза feat → возникает по факту инцидента или роста объёма редакторов
data: решить судьбу @tanstack/react-query — либо начать использовать, либо убрать → нет

## low
catalog: TS-приведение `robotsData as Robot[]` не даёт runtime-валидации — рассмотреть zod/valibot для JSON-схемы (актуально для всех 11 data-сущностей после подключения) → нет
build: рассмотреть Git LFS при росте размера иконок в data/icons/ выше 100 MB → нет
catalog: добавить недостающих мехов через overlay (Грач — свежий, отсутствует в вики; Снеговик/Стужа — сезонные, страница вики 404) → нужен ручной ввод → нет
build: включить сплит-чанки / lazy-import — main-бандл ~1.5 MiB после подключения всех 10 каталогов + blueprints иконок (варнинг rspack при production build) → фаза perf → нет (заведено по факту 2026-07-14, не блокирует)
scripts: `scripts/parser-wiki/index.ts:287` — `@typescript-eslint/no-explicit-any` (осталось от старого коммита d48ed8a, не блокирует CI) → фаза chore → нет
catalog: [blueprints] финальная привязка producesKey к конкретным вариантам целей через overlay — сейчас parser даёт translit имени без резолва ambiguity (напр. Чертеж Пулемёт → 'pulemyot', но в weapons 15+ пулемётов разного уровня); редактор указывает точную привязку через Sheets → фаза feat → нет
