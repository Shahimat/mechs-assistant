# Формат: `namespace: суть → пункт(ы) → блокеры/зависимости`
# У проекта пока нет manuals — контракт допускает отсутствие manual,
# если процедура тривиальна и покрыта README/package.json.
# Задачи сгруппированы по приоритету; порядок секций сверху вниз —
# от самого срочного к самому терпеливому. Внутри секции сверху вниз —
# от того, за что берёмся первым, к тому, что может подождать своей
# очереди.

## blocker

## high
# MVP1 · пилот-цепочка на мехах (подход C) закрыта 2026-07-12 —
# см. CHANGELOG. Роутинг + layout + catalog-примитивы (other--catalog-shell)
# выделены 2026-07-12 (по решению юзера — до второго каталога, ради
# скорости подключения следующих). Все high-пункты сделаны, дальше —
# medium: раскатать конвейер на остальные 10 каталогов через новый
# generic-паттерн.
ui: визуальная проверка UI-эффектов в браузере на проде — плитки (hover-scale/active-press/focus-ring), breadcrumbs, навигация Home→Catalogs→Мехи, каталог мехов работает как раньше; по результату обновить verified: true на `other--tile-scales-on-hover-and-navigates-on-click` → фаза audit → нужен браузер

## medium
# MVP1 · остальные 10 каталогов — накатываются через тот же конвейер
# Первый на очереди — weapons (2026-07-12). См. отдельный подсписок ниже.
# weapons подключён 2026-07-12: parser обобщён (lib/+resolvers/), 142 записи,
# UI без фильтров, роут /catalogs/weapons-catalog живой.
ui: [weapons] грамотные фильтры — по решению юзера первый заход без фильтров, настраиваем после сбора обратной связи. Кандидаты: категория (7 CSS-типов), группа орудий (4 группы), уровень (range), слот, дальность → фаза feat → нет (нужна обратная связь)
catalog: [weapons] прогнать sync:sheets и заполнить лист weapons в Google Sheets — сейчас overlay пустой (0 with overlay); наполнение = ручная работа редакторов → фаза feat → нет
catalog: остальные 9 каталогов (equipment, ammo, items, blueprints, ore, currencies, skills, loot, components) — накатываются по паттерну weapons → фаза feat → weapons раскатан
catalog: parser--wiki расширить конфиг на 9 оставшихся каталогов → фаза feat → парсер обобщён
catalog: parser--google-sheets расширить на 9 листов Sheets → фаза feat → лист под каталог настроен
ui: подключить view для остальных каталогов через catalog-примитивы (weapons, equipment, ammo, items, loot, blueprints, ore, components, currencies, skills) — плитки на CatalogsHubPage + stub-роуты уже готовы (`CatalogStubPage`), нужно заменить stub на реальный view в `src/components/catalogs/<slug>/` по мере готовности парсера/overlay каждого каталога → фаза feat → catalog-shell выделен, данные каталога загружаются
ui: подтвердить/расширить catalog-примитивы на втором каталоге — при подключении weapons проверить, какие места требуют расширения (`other--catalog-shell`), а какие копипастятся 1-в-1 → фаза refactor → второй каталог подключён
data: программный бэкап Google Sheets → отдельный GitHub Actions workflow, дампы в backups/gsheets/YYYY-MM-DD.json → фаза feat → возникает по факту инцидента или роста объёма редакторов
data: решить судьбу @tanstack/react-query — либо начать использовать, либо убрать → нет

## low
catalog: TS-приведение `robotsData as Robot[]` не даёт runtime-валидации — рассмотреть zod/valibot для JSON-схемы (актуально для всех 11 data-сущностей после подключения) → нет
build: рассмотреть Git LFS при росте размера иконок в data/icons/ выше 100 MB → нет
catalog: добавить недостающих мехов через overlay (Грач — свежий, отсутствует в вики; Снеговик/Стужа — сезонные, страница вики 404) → нужен ручной ввод → нет
build: включить сплит-чанки / lazy-import — main-бандл ~670 KiB после подключения react-router-dom и генериков (варнинг rspack при production build) → фаза perf → нет (заведено по факту 2026-07-12, не блокирует)
scripts: `scripts/parser-wiki/index.ts:287` — `@typescript-eslint/no-explicit-any` (осталось от старого коммита d48ed8a, не блокирует CI) → фаза chore → нет
