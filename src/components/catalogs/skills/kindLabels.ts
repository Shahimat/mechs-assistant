import type { SkillKind } from '@/types/skill';

/** Русские подписи kind — для chip'ов в карточке и детали. */
export const KIND_LABELS: Record<SkillKind, string> = {
  skill: 'Навык',
  ability: 'Умение',
};
